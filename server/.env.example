# Base de datos PostgreSQL
DB_USER=postgres
DB_HOST=localhost
DB_DATABASE=midosis
DB_PASSWORD=tu_password
DB_PORT=5432

# JWT
SECRET_KEY=una_clave_secreta_larga_y_aleatoria

# CORS — URL del frontend en producción (separadas por coma si hay varias)
CLIENT_URL=https://tu-app.vercel.app

# Puerto del servidor (Railway lo asigna automáticamente)
PORT=3000
